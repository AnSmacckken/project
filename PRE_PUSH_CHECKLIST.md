# قائمة التحقق قبل الرفع على GitHub

## ✅ تأكد من:

### 1. الملفات الحساسة
- [ ] لا يوجد ملف `.env` أو `.env.local` في المشروع
- [ ] جميع الملفات الحساسة في `.gitignore`
- [ ] لا توجد API keys أو secrets في الكود

### 2. الملفات الكبيرة
- [ ] `node_modules/` غير موجود (موجود في `.gitignore`)
- [ ] `.next/` غير موجود (موجود في `.gitignore`)
- [ ] لا توجد ملفات كبيرة (> 100MB)

### 3. الملفات غير الضرورية
- [ ] لا توجد ملفات `.log`
- [ ] لا توجد ملفات `.tmp` أو `.temp`
- [ ] لا توجد ملفات IDE (`.vscode/`, `.idea/`)

### 4. الملفات المطلوبة
- [x] `package.json` موجود
- [x] `README.md` موجود
- [x] `.gitignore` موجود ومحدث
- [x] `.env.example` موجود (كقالب)

### 5. الأمان
- [x] لا توجد معلومات حساسة في الكود
- [x] جميع URLs العامة فقط (لا توجد secrets)

## 🚀 جاهز للرفع!

بعد التأكد من كل شيء، استخدم:

```bash
git add .
git commit -m "Initial commit: FreshCart Ecommerce App"
git push origin main
```
